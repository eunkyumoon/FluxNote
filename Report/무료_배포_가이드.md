# FluxNote 무료 배포 가이드

이 문서는 FluxNote React 앱을 무료로 배포하는 여러 방법을 안내합니다.

---

## 🚀 추천 배포 플랫폼 비교

| 플랫폼 | 무료 티어 | 배포 난이도 | 자동 배포 | 커스텀 도메인 | 추천도 |
|--------|----------|------------|----------|--------------|--------|
| **Vercel** | ✅ 매우 관대 | ⭐ 매우 쉬움 | ✅ GitHub 연동 | ✅ 무료 | ⭐⭐⭐⭐⭐ |
| **Netlify** | ✅ 매우 관대 | ⭐ 매우 쉬움 | ✅ GitHub 연동 | ✅ 무료 | ⭐⭐⭐⭐⭐ |
| **GitHub Pages** | ✅ 완전 무료 | ⭐⭐ 쉬움 | ✅ GitHub Actions | ✅ 무료 | ⭐⭐⭐⭐ |
| **Firebase Hosting** | ✅ 관대 | ⭐⭐ 쉬움 | ✅ GitHub 연동 | ✅ 무료 | ⭐⭐⭐⭐ |
| **Surge.sh** | ✅ 완전 무료 | ⭐ 매우 쉬움 | ❌ 수동 | ❌ 유료 | ⭐⭐⭐ |

---

## 방법 1: Vercel로 배포 (가장 추천 ⭐)

### 장점
- ✅ 가장 간단하고 빠른 배포
- ✅ GitHub와 자동 연동
- ✅ 자동 HTTPS
- ✅ 무료 커스텀 도메인
- ✅ 프리뷰 배포 (PR마다)
- ✅ 무료 티어가 매우 관대함

### 배포 단계

#### 1단계: GitHub에 프로젝트 업로드

1. **GitHub 저장소 생성**
   - [GitHub](https://github.com)에 로그인
   - 새 저장소 생성 (예: `fluxnote`)

2. **로컬에서 Git 초기화 및 푸시**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: FluxNote 메모 앱"
   git branch -M main
   git remote add origin https://github.com/사용자명/fluxnote.git
   git push -u origin main
   ```

#### 2단계: Vercel 배포

1. **Vercel 가입**
   - [vercel.com](https://vercel.com) 접속
   - "Sign Up" → GitHub 계정으로 로그인

2. **프로젝트 임포트**
   - 대시보드에서 "Add New Project" 클릭
   - GitHub 저장소 선택 (`fluxnote`)
   - Vercel이 자동으로 설정 감지:
     - **Framework Preset**: Create React App
     - **Build Command**: `npm run build`
     - **Output Directory**: `build`
   - "Deploy" 클릭

3. **배포 완료**
   - 약 1-2분 후 배포 완료
   - 자동으로 `https://프로젝트명.vercel.app` URL 제공
   - 이후 코드 푸시 시 자동 재배포

### 커스텀 도메인 설정 (선택사항)

1. Vercel 대시보드 → 프로젝트 → Settings → Domains
2. 원하는 도메인 입력 (예: `fluxnote.com`)
3. DNS 설정 안내에 따라 도메인 제공업체에서 설정

---

## 방법 2: Netlify로 배포

### 장점
- ✅ Vercel과 유사한 사용성
- ✅ 폼 처리 기능 제공
- ✅ 무료 SSL 인증서
- ✅ 드래그 앤 드롭 배포 지원

### 배포 단계

#### 옵션 A: GitHub 연동 (권장)

1. **Netlify 가입**
   - [netlify.com](https://www.netlify.com) 접속
   - GitHub 계정으로 로그인

2. **프로젝트 배포**
   - "Add new site" → "Import an existing project"
   - GitHub 저장소 선택
   - 빌드 설정:
     - **Build command**: `npm run build`
     - **Publish directory**: `build`
   - "Deploy site" 클릭

#### 옵션 B: 드래그 앤 드롭 (간단)

1. **프로젝트 빌드**
   ```bash
   npm run build
   ```

2. **Netlify에 배포**
   - Netlify 대시보드 → "Add new site" → "Deploy manually"
   - `build` 폴더를 드래그 앤 드롭
   - 배포 완료!

---

## 방법 3: GitHub Pages로 배포

### 장점
- ✅ 완전 무료
- ✅ GitHub와 완벽 통합
- ✅ GitHub Actions로 자동화 가능

### 배포 단계

#### 1단계: package.json 수정

```json
{
  "homepage": "https://사용자명.github.io/fluxnote",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
```

#### 2단계: gh-pages 패키지 설치

```bash
npm install --save-dev gh-pages
```

#### 3단계: 배포

```bash
npm run deploy
```

#### 4단계: GitHub Pages 활성화

1. GitHub 저장소 → Settings → Pages
2. Source를 "gh-pages" 브랜치로 설정
3. 저장 후 `https://사용자명.github.io/fluxnote` 접속

---

## 방법 4: Firebase Hosting로 배포

### 장점
- ✅ Google 인프라
- ✅ 빠른 CDN
- ✅ 무료 SSL
- ✅ 간단한 CLI

### 배포 단계

#### 1단계: Firebase CLI 설치

```bash
npm install -g firebase-tools
```

#### 2단계: Firebase 로그인 및 초기화

```bash
firebase login
firebase init hosting
```

설정 선택:
- **What do you want to use as your public directory?**: `build`
- **Configure as a single-page app?**: `Yes`
- **Set up automatic builds and deploys with GitHub?**: `No` (또는 `Yes`)

#### 3단계: 빌드 및 배포

```bash
npm run build
firebase deploy
```

배포 완료 후 `https://프로젝트명.web.app` URL 제공

---

## 방법 5: Surge.sh로 배포 (가장 간단)

### 장점
- ✅ 매우 간단한 CLI
- ✅ 즉시 배포
- ✅ 무료 서브도메인

### 배포 단계

#### 1단계: Surge 설치

```bash
npm install -g surge
```

#### 2단계: 배포

```bash
npm run build
cd build
surge
```

첫 실행 시:
- 이메일 입력
- 비밀번호 생성
- 도메인 입력 (예: `fluxnote.surge.sh`)

---

## 📋 배포 전 체크리스트

### 필수 확인 사항

- [ ] `package.json`에 올바른 빌드 스크립트가 있는지 확인
- [ ] `.gitignore`에 `build/` 폴더가 포함되어 있는지 확인
- [ ] 환경 변수가 필요한 경우 `.env` 파일 설정
- [ ] 프로덕션 빌드 테스트: `npm run build` 후 `build` 폴더 확인

### 최적화 권장사항

1. **이미지 최적화**
   - 이미지가 있다면 압축 및 최적화

2. **코드 분할**
   - React.lazy() 사용 (이미 적용 가능)

3. **메타 태그 추가**
   - `public/index.html`에 SEO 메타 태그 추가

---

## 🔄 자동 배포 설정 (CI/CD)

### Vercel/Netlify
- GitHub에 푸시하면 자동으로 배포
- Pull Request마다 프리뷰 배포 생성

### GitHub Actions (GitHub Pages용)

`.github/workflows/deploy.yml` 파일 생성:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
```

---

## 🎯 각 플랫폼별 무료 티어 제한

### Vercel
- ✅ 대역폭: 100GB/월
- ✅ 빌드 시간: 6000분/월
- ✅ 프로젝트 수: 무제한
- ✅ 도메인: 무제한

### Netlify
- ✅ 대역폭: 100GB/월
- ✅ 빌드 시간: 300분/월
- ✅ 프로젝트 수: 무제한
- ✅ 도메인: 무제한

### GitHub Pages
- ✅ 저장소 크기: 1GB
- ✅ 대역폭: 100GB/월
- ✅ 빌드 시간: 제한 없음 (GitHub Actions 제한 적용)

### Firebase Hosting
- ✅ 저장 용량: 10GB
- ✅ 대역폭: 360MB/일
- ✅ 사이트 수: 무제한

---

## 💡 추천 배포 전략

### 개인 프로젝트 / 포트폴리오
→ **Vercel** 또는 **Netlify** (가장 간단)

### 오픈소스 프로젝트
→ **GitHub Pages** (GitHub와 완벽 통합)

### 빠른 프로토타이핑
→ **Surge.sh** (가장 빠른 배포)

### 엔터프라이즈급 안정성 필요
→ **Firebase Hosting** 또는 **Vercel**

---

## 🐛 문제 해결

### 빌드 실패
```bash
# 로컬에서 빌드 테스트
npm run build

# 문제가 있다면 로그 확인
npm run build -- --verbose
```

### 라우팅 문제 (404 에러)
- SPA(Single Page App) 설정 필요
- Vercel/Netlify: `vercel.json` 또는 `_redirects` 파일 생성

### 환경 변수
- `.env` 파일은 Git에 커밋하지 않음
- 배포 플랫폼의 환경 변수 설정에서 추가

---

## 📚 추가 리소스

- [Vercel 공식 문서](https://vercel.com/docs)
- [Netlify 공식 문서](https://docs.netlify.com)
- [GitHub Pages 가이드](https://pages.github.com)
- [Firebase Hosting 가이드](https://firebase.google.com/docs/hosting)

---

**작성일**: 2024년  
**프로젝트**: FluxNote  
**상태**: ✅ 배포 준비 완료

